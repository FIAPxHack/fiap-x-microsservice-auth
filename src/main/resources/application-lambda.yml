spring:
  main:
    lazy-initialization: true
    banner-mode: off
  # ...existing code...

# ...existing code...
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

# ...existing code...
